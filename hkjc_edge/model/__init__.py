"""Phase 2 modelling: win-probability models, exotic derivations, calibration, evaluation.

Honesty note carried from Phase 0: the public odds are a near-optimal forecaster, so a
model's only value is the INCREMENTAL information it adds over the market. Every evaluation
here compares the model against the de-vigged market probability as the benchmark to beat.
"""
